<div class="container">
  <h3><u>Current Committees of Senate</u></h3>

  <div class="row">
    <div class="col s12">

      <ul>
        {{#each model as |senate|}}
          <li> <p><b>{{senate.name}}</b></p></li>
        {{/each}}
      </ul>
    </div>
  </div>
</div>
